extends layout

block prepend title
  | #{page.title} -

block content
  != page.html
  p Listen under viser foreninger og organisasjoner som er aktive og jobber med studentmiljøet på Ifi.
  ul
    each association in env.helpers.getAssociations(contents, true)
      li
        a(href=association.url)= association.metadata.title
  h2 Tidligere foreninger eller foreningsnavn
  p Listen under viser foreninger som har vært aktive på Ifi, men som enten er lagt ned eller som har blitt omdøpt eller gjenoppstått under andre navn.
  ul
    each association in env.helpers.getAssociations(contents, false)
      li
        a(href=association.url)= association.metadata.title
