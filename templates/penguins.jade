extends layout

block prepend title
  | #{page.title} -

block vars
  - var mainclass = 'penguins';

block content
  != page.html
  h2 Voktere
  - var root = contents['structure.json'].metadata.penguins
  include ./structure.jade
