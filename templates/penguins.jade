extends layout

block prepend title
  | #{page.title} -

block vars
  - var mainclass = 'penguins';

block content
  != page.html
  h2 Voktere
  - var structureFile = contents.penguin['structure.json'].metadata
  - var individualPath = contents.penguin
  include ./structure.jade
