extends layout


block prepend title
  | #{page.title} -

block vars
  - mainclass = 'person';

block before_title
  img.profile(src='thumb.jpg')

block after_title
  - var _class = contents.class[page.metadata.class]['index.md'].metadata;
  p Tildelt #{_class.title} av Hennes Majestet Keiserpingvinen den Fornemmes orden

block content
  != page.html
  if page.metadata.reason
    p
      strong= page.metadata.reason
  if page.metadata.previous
    each classname in page.metadata.previous
      - classpage = contents.person[page.metadata.username][classname].index
      header
        h2 Tidligere tildelt: #{classpage.metadata.rank}
        p
          a(href=locals.url+'/person/year')  Tildelt #{classpage.metadata.appointed}
      != classpage.html
      p
        strong= classpage.metadata.reason
