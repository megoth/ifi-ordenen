extends layout

block prepend title
  | #{page.title} -

block vars
  - mainclass = 'person';

block content
  img.profile(src='thumb.jpg')
  p 
    strong Grad: #{page.metadata.rank} (tildelt #{page.metadata.appointed})
  != page.html
  p
    strong= page.metadata.reason
